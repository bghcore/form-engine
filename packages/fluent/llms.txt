# @bghcore/dynamic-forms-fluent

> Fluent UI v9 field components for @bghcore/dynamic-forms-core. Provides 13 editable and 6 read-only field types plus a one-line registry setup.

- Docs: [README](https://github.com/bghcore/dynamic-react-business-forms/tree/main/packages/fluent/README.md)
- Agent instructions: [AGENTS.md](https://github.com/bghcore/dynamic-react-business-forms/tree/main/packages/fluent/AGENTS.md)
- npm: [@bghcore/dynamic-forms-fluent](https://www.npmjs.com/package/@bghcore/dynamic-forms-fluent)
- License: MIT
- Peer dependencies: `react`, `react-dom`, `react-hook-form`, `@fluentui/react-components`, `@bghcore/dynamic-forms-core`

## API Surface

### Registry
- `createFluentFieldRegistry()` -- Creates the default Fluent UI v9 field registry (`Record<string, React.JSX.Element>`) for use with `InjectedFieldProvider`

### Editable Field Components (13)
- `HookTextbox` -- Single-line text input (Fluent `Input`)
- `HookNumber` -- Numeric input
- `HookToggle` -- Boolean toggle switch
- `HookDropdown` -- Single-select dropdown
- `HookSimpleDropdown` -- Simplified single-select dropdown
- `HookMultiSelect` -- Multi-select checkboxes
- `HookMultiSelectSearch` -- Multi-select with search/filter
- `HookDateControl` -- Date picker
- `HookSlider` -- Range slider
- `HookFragment` -- Dynamic fragment container
- `HookPopOutEditor` -- Expandable text editor / textarea
- `HookDocumentLinks` -- URL link CRUD
- `HookStatusDropdown` -- Status dropdown with color indicators

### Read-Only Field Components (6)
- `HookReadOnly` -- Read-only text display
- `HookReadOnlyArray` -- Read-only array/list display
- `HookReadOnlyDateTime` -- Read-only date/time display
- `HookReadOnlyCumulativeNumber` -- Read-only cumulative number display
- `HookReadOnlyRichText` -- Read-only rich text/HTML display
- `HookReadOnlyWithButton` -- Read-only text with action button

### Supporting Components
- `ReadOnlyText` -- Shared read-only text display component
- `StatusMessage` -- Error/warning/saving status display
- `HookFormLoading` -- Shimmer loading state
- `StatusColor` -- Status color indicator
- `StatusDropdown` -- Status dropdown supporting component
- `DocumentLink` -- Single document link component
- `DocumentLinks` -- Document link list component

### Helpers
- `FieldClassName()` -- Build CSS class name with optional error state
- `GetFieldDataTestId()` -- Build data-testid attribute from field metadata
- `formatDateTime()` -- Format date string for display
- `DocumentLinksStrings` -- Default string literals for document links UI

### Types
- `IReadOnlyFieldProps` -- Props for ReadOnlyText component
- `IDocumentLink` -- Document link data type

## Code Example

### Minimal Setup with Fluent UI

```tsx
import { FluentProvider, webLightTheme } from "@fluentui/react-components";
import {
  RulesEngineProvider,
  InjectedFieldProvider,
  DynamicForm,
  IFormConfig,
} from "@bghcore/dynamic-forms-core";
import { createFluentFieldRegistry } from "@bghcore/dynamic-forms-fluent";

const formConfig: IFormConfig = {
  version: 2,
  fields: {
    title: { type: "Textbox", required: true, label: "Title" },
    status: {
      type: "Dropdown",
      label: "Status",
      options: [
        { value: "draft", label: "Draft" },
        { value: "active", label: "Active" },
      ],
    },
  },
  fieldOrder: ["title", "status"],
};

function App() {
  return (
    <FluentProvider theme={webLightTheme}>
      <RulesEngineProvider>
        <InjectedFieldProvider injectedFields={createFluentFieldRegistry()}>
          <DynamicForm
            programName="myApp"
            configName="myForm"
            formConfig={formConfig}
            entityData={{ title: "", status: "draft" }}
            onSave={(data) => console.log("Saved:", data)}
          />
        </InjectedFieldProvider>
      </RulesEngineProvider>
    </FluentProvider>
  );
}
```
